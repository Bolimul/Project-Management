import { Outlet, Link} from "react-router-dom";

const Layout = () => {
    return(
        <>
        <nav>
            <div>
            <ul>

            </ul>
            </div>
            
        </nav>
        <Outlet/>
        </>
    )
};

export default Layout;